[package]
name = "rust_verify_test"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
verus-relations = { path = ".." }
rust_verify_test_macros = { path = "../../verus/source/rust_verify_test_macros" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
yansi = "0.5"

[dev-dependencies]
regex = "1"

